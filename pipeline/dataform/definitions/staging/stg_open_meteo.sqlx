config {
  type: "view",
  schema: "ro_curated",
  tags: ["staging", "daily"]
}

SELECT
  forecast_date as date,
  latitude,
  longitude,
  temperature_2m_mean as ambient_temperature_c
FROM
  `${dataform.projectConfig.defaultDatabase}`.ro_raw.open_meteo_forecast_raw
